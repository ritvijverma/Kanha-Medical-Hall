<!DOCTYPE html>
<html lang="hi">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Kanha Medical Hall | Opening Soon</title>

  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: Arial, Helvetica, sans-serif;
    }

    body {
      background: #f4fdf7;
      color: #333;
    }

    section {
      padding: 50px 20px;
      text-align: center;
    }

    h2 {
      margin-bottom: 20px;
      color: #1e8449;
    }

    /* HERO */
    .hero {
      background: linear-gradient(to right, #2ecc71, #27ae60);
      color: white;
      padding: 80px 20px;
      position: relative;
    }

    .hero h1 {
      font-size: 2.5rem;
      margin-bottom: 10px;
    }

    .hero p {
      margin-bottom: 15px;
    }

    /* LANGUAGE BUTTON */
    #langBtn {
      position: absolute;
      top: 20px;
      right: 20px;
      background: white;
      color: #27ae60;
      border: none;
      padding: 8px 16px;
      border-radius: 20px;
      font-weight: bold;
      cursor: pointer;
    }

    /* COUNTDOWN */
    .countdown {
      display: flex;
      justify-content: center;
      gap: 15px;
      flex-wrap: wrap;
    }

    .time-box {
      background: white;
      color: #27ae60;
      padding: 15px;
      border-radius: 10px;
      min-width: 90px;
    }

    /* LOCATION */
    .location button {
      background: #27ae60;
      color: white;
      border: none;
      padding: 12px 20px;
      border-radius: 5px;
      cursor: pointer;
    }

    /* SERVICES */
    .service-box {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
      gap: 20px;
      max-width: 1000px;
      margin: auto;
    }

    .card {
      background: #fff;
      padding: 20px;
      border-radius: 10px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.1);
    }

    /* CONTACT */
    .contact {
      background: #eafaf1;
    }

    .contact a {
      display: inline-block;
      margin: 10px;
      color: white;
      background: #27ae60;
      padding: 10px 18px;
      border-radius: 5px;
      text-decoration: none;
    }

    /* FOOTER */
    footer {
      background: #1e8449;
      color: white;
      padding: 15px;
      text-align: center;
    }

    /* WELCOME POPUP */
    #welcomePopup {
      position: fixed;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      background: white;
      padding: 30px 40px;
      border-radius: 15px;
      text-align: center;
      box-shadow: 0 10px 40px rgba(0,0,0,0.3);
      z-index: 9999;
      animation: popIn 0.8s ease;
    }

    #welcomePopup h2 {
      color: #27ae60;
      margin-bottom: 10px;
    }

    @keyframes popIn {
      from { transform: translate(-50%, -60%) scale(0.5); opacity: 0; }
      to { transform: translate(-50%, -50%) scale(1); opacity: 1; }
    }

    #confettiCanvas {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      pointer-events: none;
      z-index: 9998;
    }

    @media (max-width: 600px) {
      .hero h1 { font-size: 2rem; }
    }
  </style>
</head>

<body>

<!-- 🎉 WELCOME POPUP (DEFAULT HINDI) -->
<div id="welcomePopup">
  <h2 id="welcomeTitle">🎉 स्वागत है 🎉</h2>
  <p><strong id="welcomeName">कान्हा मेडिकल हॉल</strong></p>

  <p id="welcomeMsg" style="margin:10px 0; color:#27ae60; font-weight:bold;">
    5 जनवरी को हमारे<br/>
    <span style="color:#c0392b;">भव्य उद्घाटन समारोह 🎊</span><br/>
    में आपका हार्दिक स्वागत है
  </p>

  <small id="welcomeTagline">आपकी भरोसेमंद मेडिकल दुकान</small>
</div>

<canvas id="confettiCanvas"></canvas>

<!-- HERO -->
<section class="hero">
  <button id="langBtn" onclick="toggleLanguage()">English</button>

  <h1 id="pharmacyName">कान्हा मेडिकल हॉल</h1>
  <p id="tagline">आपकी भरोसेमंद मेडिकल दुकान</p>
  <p><strong id="openingText">उद्घाटन तिथि: 01 जनवरी 2026</strong></p>

  <div class="countdown">
    <div class="time-box"><h3 id="days">0</h3><span>दिन</span></div>
    <div class="time-box"><h3 id="hours">0</h3><span>घंटे</span></div>
    <div class="time-box"><h3 id="minutes">0</h3><span>मिनट</span></div>
    <div class="time-box"><h3 id="seconds">0</h3><span>सेकंड</span></div>
  </div>
</section>

<!-- LOCATION -->
<section class="location">
  <h2 id="locationTitle">📍 हमारा स्थान</h2>
<p id="addressText">
लखराव, खजूरी, किशुन गंज, खजूरी, प्रतापगढ़, उत्तर प्रदेश – 230503
</p>
  <button onclick="openMap()">दिशा प्राप्त करें</button>
</section>

<!-- SERVICES -->
<section>
  <h2 id="serviceTitle">💊 हमारी सुविधाएँ</h2>
  <div class="service-box">
    <div class="card">सभी प्रकार की दवाइयाँ उपलब्ध</div>
    <div class="card">प्रिस्क्रिप्शन दवाइयाँ</div>
    <div class="card">बीपी एवं शुगर जांच</div>
    <div class="card">आपातकालीन दवाइयाँ</div>
    <div class="card">होम डिलीवरी (जल्द आ रही है)</div>
    <div class="card">विश्वसनीय एवं असली उत्पाद</div>
  </div>
</section>

<!-- CONTACT -->
<section class="contact">
  <h2 id="contactTitle">📞 संपर्क करें</h2>
  <p>फोन: 9621104072</p>
  <a href="tel:9621104072">कॉल करें</a>
  <a href="https://wa.me/919621104072" target="_blank">WhatsApp</a>
</section>

<footer id="footerText">
  © 2026 कान्हा मेडिकल हॉल | सर्वाधिकार सुरक्षित
</footer>

<!-- JS -->
<script>
  /* COUNTDOWN */
  const openingDate = new Date("January 1, 2026 09:00:00").getTime();
  setInterval(() => {
    const d = openingDate - new Date().getTime();
    if (d < 0) return;
    days.innerText = Math.floor(d / 86400000);
    hours.innerText = Math.floor((d % 86400000) / 3600000);
    minutes.innerText = Math.floor((d % 3600000) / 60000);
    seconds.innerText = Math.floor((d % 60000) / 1000);
  }, 1000);

  function openMap() {
    window.open("https://www.google.com/maps?q=Main+Road+Near+City+Hospital");
  }

  /* LANGUAGE TOGGLE */
  let isHindi = true;

  function toggleLanguage() {
    isHindi = !isHindi;

    langBtn.innerText = isHindi ? "English" : "हिंदी";

    pharmacyName.innerText = isHindi ? "कान्हा मेडिकल हॉल" : "Kanha Medical Hall";
    tagline.innerText = isHindi ? "आपकी भरोसेमंद मेडिकल दुकान" : "Your Trusted Neighborhood Pharmacy";
    openingText.innerText = isHindi ? "उद्घाटन तिथि: 01 जनवरी 2026" : "Opening On: 01 January 2026";

    locationTitle.innerText = isHindi ? "📍 हमारा स्थान" : "📍 Our Location";
  addressText.innerText = isHindi
  ? "लखराव, खजूरी, किशुन गंज, खजूरी, प्रतापगढ़, उत्तर प्रदेश – 230503"
  : "Lakhrav, Khajuri, Kishun Ganj, Khajuri, Pratapgarh, Uttar Pradesh 230503";


    serviceTitle.innerText = isHindi ? "💊 हमारी सुविधाएँ" : "💊 Our Facilities";
    contactTitle.innerText = isHindi ? "📞 संपर्क करें" : "📞 Enquiry & Contact";

    footerText.innerText = isHindi
      ? "© 2026 कान्हा मेडिकल हॉल | सर्वाधिकार सुरक्षित"
      : "© 2026 Kanha Medical Hall | All Rights Reserved";

    /* WELCOME POPUP TEXT */
    welcomeTitle.innerText = isHindi ? "🎉 स्वागत है 🎉" : "🎉 Welcome 🎉";
    welcomeName.innerText = isHindi ? "कान्हा मेडिकल हॉल" : "Kanha Medical Hall";
    welcomeMsg.innerHTML = isHindi
      ? `5 जनवरी को हमारे<br/><span style="color:#c0392b;">भव्य उद्घाटन समारोह 🎊</span><br/>में आपका हार्दिक स्वागत है`
      : `You are invited on <span style="color:#e67e22;">5 January</span><br/>for our <span style="color:#c0392b;">Grand Opening 🎊</span>`;
    welcomeTagline.innerText = isHindi
      ? "आपकी भरोसेमंद मेडिकल दुकान"
      : "Your Trusted Neighborhood Pharmacy";
  }

  /* CONFETTI */
  const canvas = confettiCanvas, ctx = canvas.getContext("2d");
  canvas.width = innerWidth; canvas.height = innerHeight;
  const confetti = Array.from({ length: 150 }, () => ({
    x: Math.random() * canvas.width,
    y: Math.random() * canvas.height - canvas.height,
    r: Math.random() * 6 + 4,
    d: Math.random() * 10,
    c: `hsl(${Math.random() * 360},100%,50%)`
  }));

  (function draw() {
    ctx.clearRect(0, 0, canvas.width, canvas.height);
    confetti.forEach(p => {
      ctx.fillStyle = p.c;
      ctx.beginPath();
      ctx.arc(p.x, p.y, p.r, 0, Math.PI * 2);
      ctx.fill();
      p.y += p.d;
      if (p.y > canvas.height) p.y = -10;
    });
    requestAnimationFrame(draw);
  })();

  setTimeout(() => {
    welcomePopup.style.display = "none";
    confettiCanvas.style.display = "none";
  }, 8000);
</script>

</body>
</html>
